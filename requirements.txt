from flask import Flask
from threading import Thread
import os

app = Flask('')

@app.route('/')
def home():
    return "Bot aktif!"

def run():
    app.run(host='0.0.0.0', port=10000)

def keep_alive():
    t = Thread(target=run)
    t.start()

# ... senin eski kodların (import discord vs.) buraya gelecek ...
# Botun çalışmadan hemen önce keep_alive() çağır:

keep_alive()
bot.run(os.getenv('DISCORD_TOKEN'))
discord.py
